SELECT 
    *
FROM
    employees
ORDER BY first_name, last_name ASC;

#Order by descending emp_no

SELECT 
    *
FROM
    employees
ORDER BY emp_no DESC;

#Select all data from the “employees” table, ordering it by “hire date” in descending order.

SELECT 
    *
FROM
    employees
ORDER BY hire_date DESC;
SELECT * FROM employees.dept_emp_latest_date;

# viws act as a shortcut for writing the same select statement every time a new request has been made 

# occupies no extra memory

# temporary virtual data tables retrieving information from base tables
